/** Shared thresholds for nutrient adequacy colouring.
 *  >= GOOD  → green   (on target or close)
 *  >= WARN  → yellow  (partial coverage)
 *  <  WARN  → red     (well below target)
 */
export const NUTRIENT_GOOD_THRESHOLD = 80;
export const NUTRIENT_WARN_THRESHOLD = 40;
